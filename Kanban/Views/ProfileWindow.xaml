<Window x:Class="Kanban.Views.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Профиль пользователя" Height="450" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="👤 Профиль" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,25"/>

        <TextBlock Grid.Row="1" Text="Имя" FontWeight="SemiBold" Margin="0,5"/>
        <TextBox Grid.Row="2" Text="{Binding FullName}" Margin="0,5" Height="35" 
                 Padding="10" FontSize="14"/>

        <TextBlock Grid.Row="3" Text="Email" FontWeight="SemiBold" Margin="0,10,0,5"/>
        <TextBox Grid.Row="4" Text="{Binding Email, Mode=OneWay}" Margin="0,5" Height="35" 
         Padding="10" FontSize="14" IsReadOnly="True" Background="#F1F5F9"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20">
            <Button Content="Сохранить" Width="100" Height="35" Margin="5"
                    Background="#3B82F6" Foreground="White" FontWeight="SemiBold"
                    Command="{Binding SaveCommand}"/>
            <Button Content="Отмена" Width="100" Height="35" Margin="5"
                    Background="#F1F5F9" Foreground="#1E293B"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>