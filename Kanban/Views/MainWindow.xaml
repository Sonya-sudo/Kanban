<Window x:Class="Kanban.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kanban.ViewModels"
        mc:Ignorable="d"
        Title="Kanban Доска" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="BoardRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="Padding" Value="20,12,20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="#3B82F6" BorderThickness="0,0,4,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                                <Setter Property="Foreground" Value="#1D4ED8"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" 
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Left"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#EF4444"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="ColumnTemplate">
            <Border Background="#F8FAFC" CornerRadius="8" Padding="10" Width="280" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="#334155"/>
                        <TextBlock Text="{Binding Tasks.Count, StringFormat=' ({0})'}" 
                                   Foreground="#94A3B8" Margin="5,0,0,0"/>
                        <Button Content="✕" Width="20" Height="20" Margin="10,0,0,0"
                                Style="{StaticResource LinkButtonStyle}" Foreground="#EF4444"
                                Command="{Binding DataContext.DeleteColumnCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Tasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" CornerRadius="6" Padding="10" Margin="0,0,0,8"
                                            BorderBrush="#E2E8F0" BorderThickness="1">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftDoubleClick"
                                                          Command="{Binding DataContext.OpenTaskEditCommand, 
                                                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                          CommandParameter="{Binding}"/>
                                        </Border.InputBindings>
                                        <StackPanel>
                                            <Border Height="6" Width="40" CornerRadius="3" Margin="0,0,0,8"
                                                    Background="{Binding Priority}"/>
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            <TextBlock Text="⚠️ Эскалация" FontSize="10" Foreground="Red" 
                                                       Margin="0,5,0,0"
                                                       Visibility="{Binding Escalated}"/>
                                            <Button Content="Удалить" FontSize="10" Margin="0,5,0,0"
                                                    Style="{StaticResource LinkButtonStyle}" Foreground="#EF4444"
                                                    Command="{Binding DataContext.DeleteTaskCommand, 
                                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Button Grid.Row="2" Content="+ Добавить карточку" 
                            Style="{StaticResource LinkButtonStyle}" 
                            Margin="0,10,0,0" Foreground="#64748B"
                            Command="{Binding DataContext.AddTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"/>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- БОКОВАЯ ПАНЕЛЬ -->
        <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="20,30,20,20">
                    <StackPanel>
                        <Ellipse Width="60" Height="60" Fill="#E2E8F0" Stroke="#CBD5E1" StrokeThickness="1"/>
                        <TextBlock Text="{Binding UserName}" FontSize="18" FontWeight="SemiBold" Margin="0,15,0,0"/>
                        <TextBlock Text="Пользователь" FontSize="12" Foreground="#64748B"/>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="ВАШИ ДОСКИ" FontSize="11" FontWeight="Bold" 
                                   Foreground="#94A3B8" Margin="20,0,0,5"/>

                        <ItemsControl ItemsSource="{Binding Boards}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <RadioButton GroupName="BoardsGroup"
                                                 Style="{StaticResource BoardRadioButtonStyle}"
                                                 Content="{Binding Name}"
                                                 IsChecked="{Binding IsSelected}"
                                                 Command="{Binding DataContext.SelectBoardCommand, 
                                                           RelativeSource={RelativeSource AncestorType=Window}}"
                                                 CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Button Content="+ Создать доску" Margin="20,15,20,0" 
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding AddBoardCommand}"/>

                        <Button Content="🗑 Удалить доску" Margin="20,10,20,0" 
                                Style="{StaticResource SecondaryButtonStyle}"
                                Foreground="#EF4444"
                                Command="{Binding DeleteBoardCommand}"/>
                    </StackPanel>
                </ScrollViewer>

                <Button Grid.Row="2" Content="🚪 Выйти" Margin="20" Height="35"
                        Style="{StaticResource LinkButtonStyle}"
                        Command="{Binding ExitCommand}"/>
            </Grid>
        </Border>

        <!-- ОСНОВНАЯ ОБЛАСТЬ -->
        <Border Grid.Column="1" Background="Transparent">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Margin="5,0,5,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding CurrentBoardTitle}" 
                                   FontSize="24" FontWeight="Bold"/>
                        <Button Content="+ Колонка" Margin="20,0,0,0"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding AddColumnCommand}"/>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" 
                              VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding CurrentColumns}" 
                                  ItemTemplate="{StaticResource ColumnTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>