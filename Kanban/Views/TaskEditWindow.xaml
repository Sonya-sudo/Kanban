<Window x:Class="Kanban.Views.TaskEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование задачи" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Редактирование задачи" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Название" FontWeight="SemiBold"/>
        <TextBox Grid.Row="2" Text="{Binding Title}" Margin="0,5,0,15" Height="35"/>

        <TextBlock Grid.Row="3" Text="Приоритет" FontWeight="SemiBold"/>
        <ComboBox Grid.Row="4" SelectedItem="{Binding Priority}" Margin="0,5,0,15">
            <ComboBoxItem>high</ComboBoxItem>
            <ComboBoxItem>medium</ComboBoxItem>
            <ComboBoxItem>low</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="5" Text="Дедлайн" FontWeight="SemiBold"/>
        <DatePicker Grid.Row="6" SelectedDate="{Binding Deadline}" Margin="0,5,0,15"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="80" Margin="5" Command="{Binding CancelCommand}"/>
            <Button Content="Сохранить" Width="80" Margin="5" Command="{Binding SaveCommand}" 
                    Background="#3B82F6" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>